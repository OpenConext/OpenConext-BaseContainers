---
name: Build the Apache2 Shibboleth (rootless) container

on:
  push:
    paths:
      - "apache2-shibboleth-rootless/**"
      - ".github/workflows/build-apache2-shibboleth-rootless.yaml"
  schedule:
    - cron: '15 7 * * *'
  workflow_dispatch:

jobs:
  build-push-apache2-shibboleth-rootless:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: ./apache2-shibboleth-rootless
          platforms: linux/amd64,linux/arm64
          # only push the latest tag on the main branch
          push: "${{ github.ref == 'refs/heads/main' }}"
          tags: |
            ghcr.io/openconext/openconext-basecontainers/apache2-shibboleth-rootless:latest
            ghcr.io/openconext/openconext-basecontainers/apache2-shibboleth-rootless:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha
